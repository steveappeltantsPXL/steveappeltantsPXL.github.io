# GitHub API Token
# This file shows the structure for environment variables
# DO NOT commit actual tokens to the repository
# Instead, use GitHub repository secrets (Settings > Secrets and variables > Actions)

# If using GitHub Actions:
# - Add GITHUB_PAT as a repository secret
# - The deploy.yml workflow will inject it at build time

# If using local development:
# - Copy this file to .env.local
# - Add your actual token here (never commit .env or .env.local)
# - Update assets/js/app.js to read from this environment variable

GITHUB_PAT=your_personal_access_token_here